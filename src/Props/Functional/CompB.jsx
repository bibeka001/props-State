import React from 'react'

const CompB = (props) => {
  return (
    <div>
      <h1>{props.name}</h1>
      <h1>{props.age}</h1>
      <h1>{props.qualification}</h1>
    </div>
  )
}

export default CompB
